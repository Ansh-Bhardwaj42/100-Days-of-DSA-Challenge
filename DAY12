/*Problem: Write a program to check whether a given matrix is symmetric. A matrix is said to be symmetric if it is a square matrix and is equal to its 
transpose (i.e., element at position [i][j] is equal to element at position [j][i] for all valid i and j).*/

#include <stdio.h>

int main()
{
    int m, n;

    // Ask for dimensions
    printf("Enter number of rows and columns: ");
    scanf("%d %d", &m, &n);

    int matrix[m][n];

    // Input matrix elements
    printf("Enter elements of the matrix:\n");
    for(int i = 0; i < m; i++)
    {
        for(int j = 0; j < n; j++)
        {
            printf("Enter element [%d][%d]: ", i, j);
            scanf("%d", &matrix[i][j]);
        }
    }

    // Check if matrix is square
    if(m != n)
    {
        printf("Not a Symmetric Matrix");
        return 0;
    }

    int symmetric = 1;  // Assume matrix is symmetric

    // Check symmetry condition
    for(int i = 0; i < m; i++)
    {
        for(int j = 0; j < n; j++)
        {
            if(matrix[i][j] != matrix[j][i])
            {
                symmetric = 0;
                break;
            }
        }
    }

    // Print result
    if(symmetric == 1)
        printf("Symmetric Matrix");
    else
        printf("Not a Symmetric Matrix");

    return 0;
}
